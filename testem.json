{
  "launchers": {
    "mocha": {
      "command": "mocha test/*_spec.js -R tap",
      "protocol": "tap"
    }
  },

  "launch_in_dev": [ "mocha" ],
  "launch_in_ci": [ "mocha" ],
  "src_files": [
    "test/*_spec.js",
    "*/*.json"
  ]
}
